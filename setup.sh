#!/usr/bin/env bash
set -euo pipefail

# First-run setup for the hawkeye module. Runs once on the target machine
# before the module starts. Add system dependencies, model downloads, or
# permission fixes here as needed.

exit 0
